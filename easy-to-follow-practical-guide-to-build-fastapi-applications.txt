CREATING THE FASTAPI PROJECT
1. open cmd
2. uv init project_name (run this command, it creates a project directory with some files)
3. cd project_name
4. uv add fastapi[standard] (installs fastapi with additional tools in project directory)
5. code . (opens your fastapi project in vscode)

BUILDING BASIC FASTAPI APPLICATION
6. go to main.py (file in fastapi project ofcouurse)
7. clear the code that's been written there (this was filled by the uv init command)
8. import fastapi from FastAPI
9. app = FastAPI()

CREATE "/" ROUTE (ROOT route/HOME route)
10.

@app.get("/")
def home():
    return {"message" : "hellow world"}    # fastapi converts this dictionary into JSON first (automatically)

RUN YOUR FASTAPI APPLICATION i.e. START THE SERVER (fastapi applications use uvicorn ASGI server)
11. go to CLI
12. uv run fastapi dev main.py (starts the server in development/dev mode)

FASTAPI AUTOMATIC API DOCS
go to the port 8000 of ur localhost to see ur application running (http://localhost:8000/ OR http://127.0.0.1:8000/ )
go to http://127.0.0.1:8000/docs (SwaggerUI) OR http://127.0.0.1:8000/redoc (modern api docs)
u can get the exact CLI/terminal command to get the api response in your terminal as well from the api docs (swaggerUI)

*declare a list of dictionary (or write/create any dummy data, we're implementing a GET route that returns this dummy data on api endpoint)
e.g: in your main.py copy paste this (after the fastapi application creation and before the routes)👇:

posts: list[dict] = [
    {
        "id": 1,
        "author": "Aarif Mahdi",
        "title": "FastAPI is awesome",
        "content": "this framework so easy and fast",
        "date_posted": "May 16, 2026",
    },
    {
        "id": 2,
        "author": "William Wordsworth",
        "title": "Python is GOD",
        "content": "Python great for web dev",
        "date_posted": "May 17, 2026",
    }
]

CREATE "/api/posts" ROUTE (NOTE: "/api/posts" returns the posts, which is the dummy data you just created)
IMP/FYI: creating apis that return JSON data is great for programmatic access, frontend can get data this way or other services can get data too (this is FRONTEND talking to BACKEND ig)
13. 

@app.get("/api/posts")
def get_posts():
    return posts    # fastapi converts this list into JSON Array

now you can test this route (api endpoint) on your own.
you know to test a route: 
i. run the server
ii. call the endpoint (eg: http://127.0.0.1:8000/api/posts) OR go to the api docs (http://127.0.0.1:8000/docs) and try it out there!
iii. result/what to expect: you should see the data on the webpage (in this case, the posts data, that is the list of dictionary)

RETURN HTML IN BROWSER (FOR USERS)
14. from fastapi.responses import HTMLResponse (ofcouurse in main.py at top)
15. update the home route AKA "/" route (change the existing one with below code:
@app.get("/", response_class=HTMLResponse)
def home():
    // write your code here
    // return HTML data/page

(you can now test this api/route/endpoint, it should display/return HTML page in browser)
(ofcouurse 2 ways to test this route/api/endpoint are: run the server and call the correct endpoint OR go to the api docs (http://127.0.0.1:8000/docs) and try it out there!)

RETURN SAME HTML FOR 2 DIFFERENT ROUTES (OR use this knowledge to return same responses i.e. running/using same route function for different routes)
16. (just put the different routes' decorators on the same route function)
@app.get("/", response_class=HTMLResponse)
@app.get("/posts", response_class=HTMLResponse)
def home():
    return f"<h1>{posts[0]['title']}</h1>"

HIDE HTML ROUTES IN FASTAPI'S API DOCS
17. (set the parameter include_in_schema=False in the decorator of your route)
@app.get("/", response_class=HTMLResponse, include_in_schema=False)
@app.get("/posts", response_class=HTMLResponse, include_in_schema=False)
def home():
    # return {"message": "Hello, World!"}  # fastapi converts this dictionary into JSON first (automatically)
    return f"<h1>{posts[0]['title']}</h1>"

USE JINJA2 TEMPLATES (to serve our HTML pages to users, maintaining our JSON endpoints for backend APIs)
18. from fastapi import Request
19. from fastapi.templating import Jinja2Templates
(remove the line: from fastapi.responses import HTMLResponse) cuz we ain't using the html responses anymore , we r gonna use the templating (which is the right way to do it)

*create a folder named "templates" in project (will use this directory to create templates object in our main.py, this templates object will be used by fastapi to look for templates files in the directory "templates")
20. (write this line below your fastapi app creation and above the posts dummy data)
templates = Jinja2Templates(directory="templates")
